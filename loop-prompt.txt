/loop Continue the Tier 3 native terminal renderer. Run `git branch --show-current` — it must be `native-renderer` (if not, `git checkout native-renderer`). Read docs/native-renderer.md and `git log --oneline -8` to orient: M1 is done, next is M2a. Work M2 in order — M2a: align the native surface to the real terminal pane (frontend reports pane bounds+dpr via a new Tauri command, backend repositions/sizes/clips the NSView and reconfigures the wgpu surface on resize, add a redraw path); M2b: feed the server byte stream into alacritty_terminal's Term grid; M2c: glyph atlas + instanced wgpu cell renderer. I am ASLEEP and cannot look at the screen, so you cannot visually verify GPU output. For each step: write it, then pass `cargo check`, `cargo clippy -p vosh-app -- -D warnings`, `cargo fmt --check`, `npm run typecheck`, `npm run lint`, `npm run format:check`, then `npm run tauri build`, and commit the working step on native-renderer with a clear message. cargo may not be on PATH — run `source $HOME/.cargo/env` first. Do NOT push and do NOT tag. When a step can only be confirmed visually, commit the code, write the visual checkpoint into the commit body and docs/native-renderer.md, and move to the next codeable step. If you hit a real architectural decision or are genuinely blocked, stop, summarize, and wait. Keep commits small. Self-pace.
